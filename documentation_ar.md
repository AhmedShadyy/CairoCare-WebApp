# دليل توثيق مشروع Cairo Care الشامل

هذا الملف يحتوي على شرح مفصل لجميع ملفات وأجزاء مشروع "Cairo Care" (نظام الرعاية الصحية الذكي). هذا الدليل مخصص للمطورين لفهم هيكلة المشروع ووظيفة كل ملف في النسخة الأخيرة.

---

## 1. الهيكل العام للمشروع (Project Structure)
المشروع مبني باستخدام **Node.js** و **Express** كـ Backend، و **HTML/CSS/JS (Vanilla)** كـ Frontend، وقاعدة بيانات **MySQL**.

### المجلدات الرئيسية:
- **`public/`**: يحتوي على جميع الملفات التي تظهر للمستخدم (الصفحات، الصور، ملفات الـ JS و CSS).
- **`src/`**: يحتوي على "منطق" الخادم (Server Logic) والكود الخلفي (Backend Code).
- **`uploads/`**: المجلد الذي يتم فيه تخزين الصور والملفات المرفوعة من قبل المستخدمين.

---

## 2. ملفات الخادم وقاعدة البيانات (Backend & Database)

### `server.js`
- **الوظيفة**: هو نقطة البداية (Entry Point) للمشروع.
- **الشرح**:
    - يقوم بتشغيل خادم الويب (Express Server).
    - يربط المسارات (Routes) الخاصة بالمشروع (مثل تسجيل الدخول، المواعيد، إلخ).
    - يقوم بتقديم الملفات الثابتة (Static Files) من مجلد `public`.
    - يبدأ الاستماع على المنفذ (Port) المحدد (غالباً 3000).

### `src/config/db.js`
- **الوظيفة**: الاتصال بقاعدة البيانات.
- **الشرح**: ينشئ اتصالاً مع قاعدة بيانات MySQL باستخدام المكتبة `mysql2`. يتم استخدامه في كل الملفات الأخرى التي تحتاج للتعامل مع البيانات.

### `schema.sql`
- **الوظيفة**: هيكل قاعدة البيانات.
- **الشرح**: يحتوي على أوامر SQL لإنشاء الجداول الضرورية:
    - `users`: لتخزين بيانات المستخدمين (مرضى، أطباء، مشرفين).
    - `doctors_profile`: لتخزين تفاصيل الأطباء الإضافية (مثل التخصص، السعر، المواعيد).
    - `appointments`: لتخزين المواعيد المحجوزة وتفاصيلها وحالتها.

### `package.json`
- **الوظيفة**: مدير الحزم (Package Manager).
- **الشرح**: يحتوي على قائمة المكتبات المستخدمة في المشروع (مثل express, mysql2, bcryptjs, multer) وأوامر التشغيل (scripts).

---

## 3. الواجهة الأمامية (Frontend Pages) - مجلد `public`

### الصفحات العامة:
1.  **`index.html`**: الصفحة الرئيسية للموقع. تحتوي على نبذة عن الموقع، الخدمات، وخيارات تسجيل الدخول.
2.  **`login.html`**: صفحة تسجيل الدخول.
3.  **`register.html`**: صفحة إنشاء حساب جديد (مريض أو طبيب).

### لوحات التحكم (Dashboards):
4.  **`dashboard/patient.html`**:
    - لوحة تحكم المريض.
    - تتيح للمريض البحث عن الأطباء، حجز موعد، رفع ملفات طبية، وعرض تاريخ مواعيده (السجل الطبي).
5.  **`dashboard/doctor.html`**:
    - لوحة تحكم الطبيب.
    - تتيح للطبيب إدارة ملفه الشخصي، تحديد مواعيد العمل، وعرض المواعيد القادمة وتفاصيل المرضى.
6.  **`dashboard/admin.html`**:
    - لوحة تحكم المشرف (Admin).
    - تتيح إدارة النظام بالكامل: إدارة المستخدمين (حذف/تعديل)، الموافقة على الأطباء الجدد، وعرض إحصائيات النظام.

---

## 4. ملفات التنسيق والجافاسكريبت (Assets) - مجلد `public/assets`

### التنسيق (CSS):
- **`css/style.css`**: الملف الرئيسي لتصميم الموقع.
    - يحتوي على الألوان، الخطوط، وتنسيق جميع الصفحات.
    - يدعم الوضع الليلي (Dark Mode).
    - يدعم اللغة العربية (RTL - Right to Left).
    - يتضمن تنسيقات الخلفيات المخصصة لكل صفحة.

### الجافاسكريبت (JS):
1.  **`js/main.js`**:
    - **أهم ملف JS في المشروع**. يحتوي على الدوال المشتركة بين كل الصفحات.
    - يدير عملية الترجمة (Arabic/English).
    - يدير التبديل بين الوضع الليلي والنهاري.
    - يتحقق من تسجيل الدخول (Authentication Check) ويحدث شريط التنقل (Navbar).

2.  **`js/auth.js`**:
    - يدير عمليات تسجيل الدخول والتسجيل (إرسال البيانات للخادم، التعامل مع الأخطاء، حفظ التوكن).

3.  **`js/patient.js`**:
    - يحتوي على "ذكاء" لوحة تحكم المريض.
    - دوال لجلب قائمة الأطباء، تصفية النتائج، إرسال طلب حجز، وعرض المواعيد السابقة.

4.  **`js/doctor.js`**:
    - يحتوي على "ذكاء" لوحة تحكم الطبيب.
    - دوال لحفظ الملف الشخصي، تحديث الحالة، وعرض طلبات الحجز القادمة واتخاذ إجراءات عليها (قبول/رفض/إكمال).

5.  **`js/admin.js`**:
    - يحتوي على "ذكاء" لوحة تحكم المشرف.
    - دوال لجلب كل المستخدمين، حذفهم، والموافقة على الأطباء المعلقين.

---

## 5. الكود الخلفي والمتحكمات (Backend Controllers) - مجلد `src/controllers`

هذه الملفات هي "العقل المدبر" الذي يعالج الطلبات القادمة من الواجهة الأمامية:

1.  **`authController.js`**:
    - يعالج تسجيل الدخول والتسجيل.
    - يقوم بتشفير كلمات المرور (Hashing) وإصدار رموز الدخول (JWT Tokens).

2.  **`patientController.js`**:
    - يعالج طلبات المرضى (مثل البحث عن أطباء).

3.  **`doctorController.js`**:
    - يعالج طلبات الأطباء (تحديث الملف الشخصي، جلب المواعيد الخاصة بالطبيب).

4.  **`appointmentController.js`**:
    - يدير عملية الحجز بالكامل.
    - يحفظ الموعد في قاعدة البيانات، يربط المريض بالطبيب، ويعالج رفع الملفات المرفقة مع الموعد.
    - يدير تغيير حالة الموعد (مؤكد، ملغى، مكتمل).

5.  **`adminController.js`**:
    - يوفر البيانات للمشرف (قوائم المستخدمين، الإحصائيات).

---

## 6. ملخص أدوار المستخدمين

- **المريض (Patient)**: يسجل -> يبحث عن طبيب -> يحجز موعد (مع وصف وصور) -> يتابع الحالة.
- **الطبيب (Doctor)**: يسجل -> ينتظر موافقة الأدمن -> يكمل ملفه الشخصي -> يستقبل مواعيد -> يعالج المرضى.
- **المشرف (Admin)**: يراقب النظام -> يوافق على الأطباء -> يتدخل عند الحاجة (إدارة المستخدمين).

---
*تم إنشاء هذا التوثيق بتاريخ 2025-12-18 ليكون مرجعاً نهائياً للمشروع.*
