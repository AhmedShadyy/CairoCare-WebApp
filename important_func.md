# شرح تفصيلي للدوال والملفات (Important Functions)

هذا الملف يحتوي على شرح تقني مفصل لأهم الدوال (Functions) المستخدمة في ملفات الجافاسكريبت (Frontend & Backend) وشرح ملف التكوين (JSON).

---

## 1. ملفات الخادم (Server & Controllers)

### أ. ملف `server.js` (نقطة البداية)
- **`app.listen(PORT)`**:
    - **الوظيفة**: تشغيل الخادم والبدء في استقبال الطلبات على المنفذ المحدد.
- **`express.static('public')`**:
    - **الوظيفة**: جعل مجلد `public` متاحاً للعامة، مما يسمح للمتصفح بتحميل ملفات CSS و JS والصور مباشرة.
- **`path.join(__dirname, ...)`**:
    - **الوظيفة**: دمج مسارات الملفات بشكل صحيح لضمان عملها على أي نظام تشغيل (Windows/Linux).

### ب. المتحكمات (Controllers) - المسؤولة عن المنطق

#### `authController.js` (التحقق من الهوية)
- **`register(req, res)`**:
    - تستقبل بيانات المستخدم الجديد.
    - تقوم بتشفير كلمة المرور باستخدام `bcrypt`.
    - تحفظ المستخدم في قاعدة البيانات.
- **`login(req, res)`**:
    - تبحث عن المستخدم بالبريد الإلكتروني.
    - تقارن كلمة المرور المدخلة مع المشفرة.
    - إذا صحيحة، تنشئ "Token" (تصريح دخول) وتعيده للمستخدم.

#### `appointmentController.js` (إدارة المواعيد)
- **`bookAppointment(req, res)`**:
    - تستقبل طلب الحجز (المريض، الطبيب، التاريخ، الوقت).
    - تحفظ صورة الملف الطبي (إن وجدت) في مجلد `uploads`.
    - تنشئ سجلاً جديداً في جدول `appointments`.
- **`updateStatus(req, res)`**:
    - تسمح للطبيب بتغيير حالة الموعد (مكتمل/ملغى).

---

## 2. ملفات الجافاسكريبت (Frontend JavaScript)

### أ. ملف `main.js` (الدوال الأساسية للموقع)
هذا الملف هو العمود الفقري للواجهة الأمامية.

- **`checkAuth()`**:
    - **الوظيفة**: تعمل فور تحميل الصفحة. تتأكد هل المستخدم مسجل دخول أم لا (عن طريق فحص `localStorage`).
    - **النتيجة**: إذا كان مسجلاً، تعرض اسمه وزر الخروج. إذا لا، تعرض زري "دخول" و"تسجيل".
- **`translatePage()`**:
    - **الوظيفة**: تحويل لغة الموقع (عربي/إنجليزي).
    - **الآلية**: تبحث عن كل عنصر يحمل الصفة `data-i18n` وتستبدل نصه بالنص المقابل من قاموس الترجمة.
- **`toggleTheme()`**:
    - **الوظيفة**: التبديل بين الوضع الليلي (Dark Mode) والنهاري.
    - **الآلية**: تضيف أو تزيل الكلاس `dark-mode` من وسم `<body>` وتحفظ التفضيل في المتصفح.
- **`logout()`**:
    - **الوظيفة**: تسجيل الخروج.
    - **الآلية**: تحذف التوكن وبيانات المستخدم من `localStorage` وتعيد توجيه المستخدم لصفحة الدخول.

### ب. ملف `patient.js` (خاص بالمرضى)
- **`loadDoctors()`**:
    - تجلب قائمة الأطباء المتاحين من الخادم (`/api/doctors`).
    - تنشئ بطاقات (Cards) لكل طبيب تعرض (اسمه، صورته، تخصصه) وتعرضها في الصفحة.
- **`bookAppointment(doctorId)`**:
    - تفتح نافذة الحجز (Modal).
    - عند التأكيد، ترسل بيانات الحجز (بما في ذلك الصور والوصف) إلى الخادم.

### ج. ملف `doctor.js` (خاص بالأطباء)
- **`loadAppointments()`**:
    - تجلب المواعيد الخاصة بالطبيب الحالي.
    - تعرضها في جدول يحتوي على اسم المريض، التاريخ، والحالة.
- **`updateAppointmentStatus(id, status)`**:
    - ترسل طلباً للخادم لتحديث حالة موعد معين (مثلاً عند ضغط الطبيب على "إكمال الزيارة").

---

## 3. ملف `package.json` (ملف الجوسون)

هذا الملف هو "هوية المشروع" ويحتوي على المعلومات الوصفية والإعدادات.

### الأقسام المهمة:
- **`"dependencies"`**:
    - تسرد المكتبات التي يحتاجها المشروع ليعمل:
        - `express`: إطار عمل الويب.
        - `mysql2`: للاتصال بقاعدة البيانات.
        - `bcryptjs`: لتشفير كلمات المرور وحمايتها.
        - `jsonwebtoken`: لإنشاء رموز التحقق وتأمين الجلسات.
        - `multer`: لمعالجة رفع الملفات والصور.

- **`"scripts"`**:
    - تحتوي على اختصارات لتشغيل المشروع:
        - **`"start": "node server.js"`**: لتشغيل الخادم في الوضع العادي.
        - **`"dev": "nodemon server.js"`**: لتشغيل الخادم في وضع التطوير (يعيد التشغيل تلقائياً عند تعديل الكود).

---
*تم إعداد هذا الشرح ليكون مرجعاً تقنياً لكيفية عمل الدوال والملفات بداخل نظام Cairo Care.*
